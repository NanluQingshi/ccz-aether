# 后端开发问题

## 1. MyBatis-Plus Mapper 泛型类型推断失败

**错误信息**
```
BeanDefinitionStoreException: Invalid bean definition with name 'categoryMapper'
Invalid value type for attribute 'factoryBeanObjectType': java.lang.String
```

**原因**：MyBatis-Plus 通用版 starter 与 Spring Boot 3.3 存在兼容性问题，泛型推断时触发 Bug。

**解决**：将 `pom.xml` 中的依赖替换为 Spring Boot 3 专用版：
```xml
<!-- 替换前 -->
<artifactId>mybatis-plus-boot-starter</artifactId>

<!-- 替换后 -->
<artifactId>mybatis-plus-spring-boot3-starter</artifactId>
```

---

## 2. PaginationInnerInterceptor 类找不到

**错误信息**
```
cannot find symbol: class PaginationInnerInterceptor
location: package com.baomidou.mybatisplus.extension.plugins.inner
```

**原因**：MyBatis-Plus 3.5.9 将分页插件拆分到独立模块，`mybatis-plus-extension` 中不再包含。

**解决**：额外引入分页插件依赖：
```xml
<dependency>
    <groupId>com.baomidou</groupId>
    <artifactId>mybatis-plus-jsqlparser</artifactId>
    <version>3.5.9</version>
</dependency>
```

---

## 3. @Select 注解返回自定义 VO 导致 Bean 解析失败

**原因**：Spring 解析 Mapper Bean 类型时，对带非实体返回类型的 `@Select` 方法处理存在边界问题。

**解决**：将 `@Select` 中的 SQL 移入 XML Mapper 文件，接口只保留方法签名：
```java
// Mapper 接口：去掉 @Select，只保留签名
List<CategoryVO> selectWithPostCount();
```
在 `resources/mapper/CategoryMapper.xml` 中写 SQL，并确保 `application.yml` 配置了扫描路径：
```yaml
mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
```

---

## 4. 接口异常无日志输出

**现象**：接口返回 500，但后端终端没有任何错误日志。

**原因**：`GlobalExceptionHandler` 捕获了所有异常但只返回响应，没有打印日志。

**解决**：在 `handleException` 方法中加上日志输出：
```java
@ExceptionHandler(Exception.class)
@ResponseStatus(HttpStatus.INTERNAL_SERVER_ERROR)
public ApiResponse<Void> handleException(Exception e) {
    log.error("Unhandled exception", e);
    return ApiResponse.error(500, "服务器内部错误: " + e.getMessage());
}
```

---

## 5. getStats 接口报错：postTagMapper.selectCount 失败

**原因**：`PostTag` 实体没有 `@TableId` 注解，MyBatis-Plus 的 `selectCount` 在无主键实体上执行会报错。此外逻辑本身也有误，统计标签数应用 `tagMapper` 而不是 `postTagMapper`。

**解决**：将 `PostService.getStats()` 中的统计改为：
```java
stats.setTotalTags(tagMapper.selectCount(null));
```
