# 环境与构建问题

## 1. Maven 找不到 spring-boot 插件

**错误信息**
```
No plugin found for prefix 'spring-boot' in the current project
```

**原因**：在错误目录执行命令，当前目录下没有 `pom.xml`。

**解决**：必须在 `backend/` 目录下执行：
```bash
cd backend
mvn spring-boot:run
```

---

## 2. JDK 版本冲突导致编译失败

**错误信息**
```
Fatal error compiling: java.lang.ExceptionInInitializerError:
com.sun.tools.javac.code.TypeTag :: UNKNOWN
```

**原因**：Maven 使用了 Homebrew 安装的 JDK 25，项目要求 Java 17，版本不兼容。

**解决**：在 `~/.zshrc` 中显式导出 JDK 17 路径：
```bash
export JAVA_HOME=/Library/Java/JavaVirtualMachines/temurin-17.jdk/Contents/Home
export PATH="$JAVA_HOME/bin:$PATH"
```
然后 `source ~/.zshrc`，用 `mvn -version` 验证是否为 17。

**注意**：`JAVA_HOME=...` 缺少 `export` 时不会被子进程继承，必须加 `export`。

---

## 3. 端口被占用导致后端启动失败

**错误信息**
```
Web server failed to start. Port 8080 was already in use.
```

**原因**：上一次后端进程没有正常退出（如直接关闭终端窗口），进程仍在后台占用端口。

**解决**：
```bash
# 方法一：杀掉占用 8080 的进程
lsof -ti :8080 | xargs kill

# 方法二：换端口启动
mvn spring-boot:run -Dspring-boot.run.arguments=--server.port=8081
```

---

## 4. create-vite 版本与 Node.js 不兼容

**错误信息**
```
SyntaxError: The requested module 'node:util' does not provide an export named 'styleText'
```

**原因**：`create-vite@9.x` 要求 Node.js `>=20.19.0`，本机为 v18。

**解决**：指定兼容版本：
```bash
npm create vite@5 frontend -- --template react-ts
```
